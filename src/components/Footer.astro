<footer class="glass-footer">
  <div class="footer-container">
    <div class="footer-content">
      <div class="footer-brand">
        <h3>Paula Palmer</h3>
        <p>Celebrant & Life Events</p>
      </div>

      <div class="footer-socials">
        <a
          href="https://www.facebook.com/Paula-Palmer-Celebrant-704159899649642/"
          aria-label="Facebook">FB</a
        >
      </div>
    </div>

    <div class="footer-bottom">
      <p>&copy; 2026 Paula Palmer. All rights reserved.</p>
      <button id="back-to-top" class="back-to-top" aria-label="Scroll to top">
        ↑
      </button>
    </div>
  </div>
</footer>

<script>
  // Simple Back to Top logic
  const backToTop = document.getElementById("back-to-top");
  backToTop?.addEventListener("click", () => {
    window.scrollTo({ top: 0, behavior: "smooth" });
  });

  const magneticElements = document.querySelectorAll(
    ".footer-socials a, .back-to-top",
  );

  magneticElements.forEach((el) => {
    el.addEventListener("mousemove", (e) => {
      const { left, top, width, height } = el.getBoundingClientRect();

      // Calculate the center of the element
      const centerX = left + width / 2;
      const centerY = top + height / 2;

      // Calculate distance from center to mouse
      // We divide by a factor (e.g., 2) so the movement is subtle
      const moveX = (e.clientX - centerX) / 2;
      const moveY = (e.clientY - centerY) / 2;

      // Apply the pull
      el.style.transform = `translate(${moveX}px, ${moveY}px)`;
    });

    // Reset position when mouse leaves
    el.addEventListener("mouseleave", () => {
      el.style.transform = `translate(0px, 0px)`;
    });
  });
</script>
